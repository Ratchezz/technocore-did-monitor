{
  "name": "technocore-did-monitor",
  "version": "2.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node agent.mjs",
    "syntax": "node --check agent.mjs && node --check verify-receipt.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
